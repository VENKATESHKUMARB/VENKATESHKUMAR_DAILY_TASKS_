<configuration>
 
    <!-- Console Appender (optional, logs to console) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>
 
    <!-- File Appender (logs to file) -->
    <appender name="FILE" class="ch.qos.logback.core.FileAppender">
        <file>logs/test-log.txt</file> <!-- Output file location -->
        <append>true</append> <!-- Append to the file, do not overwrite it -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>
 
    <!-- Root Logger Configuration -->
    <root level="info">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/> <!-- Reference the file appender -->
    </root>
 
</configuration>